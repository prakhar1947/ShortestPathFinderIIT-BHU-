# Port for Flask server (Render/Railway sets PORT automatically)
PORT=5010

# Frontend URL allowed by CORS (e.g. https://shortest-path-finder-delta.vercel.app or * for all)
FRONTEND_URL=https://shortest-path-finder-delta.vercel.app
